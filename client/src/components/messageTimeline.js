import React from "react";
import MessageList from "../containers/MessageList";

export default (props)=>{
    return (
        <div className="row">
            <MessageList/>
        </div>
    )
}
